# Setup & Run (Local)

## Prerequisites
- Node.js (LTS)
- Git
- (Optional) Postman / Thunder Client

## Project Structure (important paths)
- backend/src/server.js serves the API and also serves the static frontend folder.
- frontend/ contains index.html and game.html.

## Run Backend
From the project root:

```bash
cd backend
npm install
npm run dev